# Общие настройки для ЛЮБОГО профиля.
spring:
  application:
    name: practice-3
  profiles:
    active: dev

server:
  port: 8080

app:
  name: "Task Manager"
  description: "Учебный сервис управления задачами (Practice 3)"
  max-tasks: 50
  notification:
    type: console
